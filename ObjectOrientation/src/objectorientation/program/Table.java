package objectorientation.program;

 class Table {

	 int seatingcapacity;
	 int cost;
	 String  color;
	 String Brand;
	 
	 
	 void allowToset() {
		 System.out.println("usr can set on the table which is "+color);
	 }
	 void allowTowrite() {
		 System.out.println("User can write in the book by keeping it on the which costs is "+cost+"(rs)"
				 );
		 
	 }
	 
}
